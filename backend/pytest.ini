[tool:pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts =
    --verbose
    --tb=short
    --strict-markers

markers =
    unit: Unit tests
    integration: Integration tests
    auth: Authentication tests